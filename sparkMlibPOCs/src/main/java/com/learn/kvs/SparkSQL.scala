package com.learn.kvs
/**
 * this program to read '|' delimited data with header, parse header and load into an array
 * remove header from RDD and create Data Frame.
 * try the save the above RDD in Json, Labeled objects. 
 */
object SparkSQL {
  
}